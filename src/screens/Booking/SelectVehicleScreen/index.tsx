import { Text, View } from "react-native";

const SelectVehicleScreen = ({ navigation }: any) => {
  return (
    <View style={{ flex: 1, justifyContent: "flex-start" }}>
      <Text>Vehicl eScreen</Text>
    </View>
  );
};

export default SelectVehicleScreen;
